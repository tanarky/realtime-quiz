<!DOCTYPE html>
<html>
  <head>
    <title>Quiz Admin</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css" integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous">
    <link href="assets/css/application.css" rel="stylesheet" media="screen">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
    <div class="container">
      <h1>Quiz Admin</h1>

      <table class="table">
        <thead>
          <tr>
            <th>ID</th>
            <th>問題</th>
            <th>選択肢</th>
            <th>正解</th>
          </tr>
        </thead>
        <tbody>
          <% @qa.each do |k, v| %>
          <tr id="<%= k %>">
            <td class="qid"><%= k %></td>
            <td class="body"><%= v[:body] %></td>
            <td class="choices"><%= v[:choices].join('/') %></td>
            <td class="answer"><%= v[:choices][v[:answer]] %></td>
          </tr>
          <% end %>
        </tbody>
      </table>

      <form id="questions" class="form-inline">
        <label>問題・・・</label>
        <select id="ready-go" class="custom-select" name="answer">
          <% @qa.each do |k, v| %>
          <option value="<%= k %>"><%= k %></option>
          <% end %>
        </select>
        <button class="btn btn-primary" type="submit">Send</button>
      </form>

      <hr>

      <form id="answers" class="form-inline">
        <label>正解は・・・</label>
        <select class="custom-select answer" name="answer">
          <% @qa.each do |k, v| %>
          <option value="<%= k %>"><%= k %></option>
          <% end %>
        </select>
        <button class="btn btn-primary" type="submit">Send</button>
      </form>

      <hr>

      <h2>出題中</h2>
      <div id="now-question"></div>

      <hr>

      <h2>答え</h2>
      <div id="now-answer"></div>

    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>

    <script type="text/javascript" src="assets/js/admin.js"></script>
  </body>
</html>
